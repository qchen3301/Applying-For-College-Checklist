const router = require('express').Router()
const {ActionItems} = require('../db/model')

//Show all/show one will be handled by React on the front end

//Create


//Update

//Delete

module.exports = router